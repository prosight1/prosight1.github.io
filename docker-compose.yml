services:
  allconfig-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: allconfig_showcase_web
    restart: always
    ports:
      - "3003:3003"
    environment:
      NODE_ENV: production
      PORT: 3003
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3003"]
      interval: 30s
      timeout: 5s
      retries: 3